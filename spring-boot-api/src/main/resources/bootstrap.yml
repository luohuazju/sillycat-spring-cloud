server:
  address: 0.0.0.0
  port: 9528
  servlet:
    context-path: /springbootapi
spring:
  profiles:
    active: dev
  application:
    name: springbootapi
  cloud:
    consul:
      host: centos7-master
      port: 80
      discovery:
        serviceName: springbootapi
        healthCheckUrl: http://${server.address}:${server.port}/springbootapi/actuator/health
        instanceId: ${spring.application.name}-${server.address}-${server.port}-${spring.profiles.active}
        healthCheckInterval: 5s
        healthCheckCriticalTimeout: 10m
        tags: 
          - traefik.enable=true 
          - traefik.http.routers.springbootapi.rule=Host(`springbootapi.sillycat.com`)
          - traefik.http.services.nodestarter.loadbalancer.server.port=9527
      config:
        import-check:
          enabled: false
        enabled: true 
        format: yaml 
        prefix: config
        profile-separator: ':'
        data-key: data
